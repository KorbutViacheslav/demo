{
   "id": "$context.result.id.S",
   "createdAt": "$context.result.createdAt.S"
}
#if($context.result.id)
{
   "id": "$context.result.id",
   "createdAt": "$context.result.createdAt"
}
#else
$utils.error("Failed to create event")
#end